#!/usr/bin/env bash
set -euo pipefail

REGISTRY="registry:5000"
IMAGE="${REGISTRY}/vector-kv:latest"

echo "Building ${IMAGE}..."
docker build -t "${IMAGE}" .

echo "Pushing ${IMAGE}..."
docker push "${IMAGE}"

echo "Done."
